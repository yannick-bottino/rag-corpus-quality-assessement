llm:
  provider: mock
  base_url: ""
  model: "mock-model"
  temperature: 0
  api_key_env: "CQG_LLM_API_KEY"
paths:
  workdir: "./workdir"
thresholds:
  coverage_flag_below: 0.7
parsing:
  use_docling: false   # pypdf par defaut (corpus ne-numerique) ; passer a true pour activer Docling (docs riches en tableaux, hardware capable)
